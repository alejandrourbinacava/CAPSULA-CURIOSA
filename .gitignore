# deps
node_modules/
# secretos (las API keys van en GitHub Secrets, NUNCA en el repo)
.env
# salidas de render
out/
# temporales de generación de voz
public/voz1deep/_raw/
public/voz1full/_raw/
public/voz1/_raw/
# logs
*.log
npm-debug.log*
# --- pipeline spec: regenerables (NO se commitean; se recrean en cada run) ---
# audio de voz por episodio (pesado; lo regenera 01-tts)
episodes/**/*.mp3
episodes/**/words.json
# assets descargados (Wikipedia/Iconify) - los rebaja 01-assets
public/assets/
# audio del render activo (se recopia en cada build). scenes.json SÍ se versiona.
public/active/audio.mp3
